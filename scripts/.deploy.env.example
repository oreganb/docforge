# Deploy credentials for scripts/deploy-ftp.py
# Copy this file to `scripts/.deploy.env` (git-ignored) and fill in real values.
# Never commit the real .deploy.env.

FTP_HOST=ftp.example-host.com
FTP_USER=your-ftp-user
FTP_PASS=your-ftp-password
REMOTE_BASE=/webspace/httpdocs/your-site
